'use client';

export function PoolList({ compact = false }: { compact?: boolean }) {
  return (
    <div className="bg-darker rounded-xl p-6">
      <h2 className="text-xl font-bold mb-4">Available Pools</h2>
      {/* Pool list implementation */}
      <div className="text-gray-400">Pool list component</div>
    </div>
  );
}
